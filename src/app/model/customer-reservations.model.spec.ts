import { CustomerReservations } from './customer-reservations.model';

describe('CustomerReservations', () => {
  it('should create an instance', () => {
    expect(new CustomerReservations()).toBeTruthy();
  });
});
